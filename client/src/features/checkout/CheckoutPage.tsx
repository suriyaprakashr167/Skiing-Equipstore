import { Typography } from "@mui/material";

export default function CheckoutPage() {
  return (
    <Typography variant="h3">
        Only authorised user should be able to see this.
    </Typography>
  )
}